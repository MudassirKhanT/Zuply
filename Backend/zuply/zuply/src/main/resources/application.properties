debug=true

# Server
server.port=9090

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/zuply_db
spring.datasource.username=root
spring.datasource.password=Suvetha@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
app.jwt.secret=zuply_secret_key_change_this
app.jwt.expiration-ms=604800000

# Disable default Spring Security login
spring.security.user.name=disable
spring.security.user.password=disable