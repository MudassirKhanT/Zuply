# ── Node modules ──────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Angular build output ──────────────────────────────
dist/
tmp/
out-tsc/
bazel-out/

# ── Angular cache ─────────────────────────────────────
.angular/
.angular/cache/

# ── Environment files (API keys, secrets) ─────────────
src/environments/environment.prod.ts
.env
.env.local
.env.production
Backend/.env

# ── .env.example is safe to push (contains no real secrets) ──
!Backend/.env.example

# ── IDE files ─────────────────────────────────────────
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── OS files ──────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Test coverage ─────────────────────────────────────
coverage/
.nyc_output/

# ── Logs ──────────────────────────────────────────────
logs/
*.log

# ── Package lock (optional — keep if using npm) ───────
# package-lock.json
.vercel
.env*.local

# ── Claude AI session files ───────────────────────────
.claude/
CLAUDE.md

# ── Backend sensitive config & uploads ────────────────
Backend/src/main/resources/application.properties
Backend/uploads/

# ── Frontend production environment (has URLs) ─────────
Frontend/Zuply-FrontEnd/environments/environment.prod.ts
